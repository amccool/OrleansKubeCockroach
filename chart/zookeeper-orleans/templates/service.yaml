apiVersion: v1
kind: Service
metadata:
  name: {{ include "zookeeper-orleans.fullname" . }}
  labels:
    {{- include "zookeeper-orleans.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zookeeper-orleans.selectorLabels" . | nindent 4 }}
