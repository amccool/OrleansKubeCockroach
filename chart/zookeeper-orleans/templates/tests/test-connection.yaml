apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zookeeper-orleans.fullname" . }}-test-connection"
  labels:
    {{- include "zookeeper-orleans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zookeeper-orleans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
